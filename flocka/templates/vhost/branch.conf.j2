server {
    listen       80;
    server_name  {{ branch }}.{{ hostname }};

    location / {
        proxy_pass   http://127.0.0.1:{{ port }}$request_uri;
        proxy_set_header Host $host;
    }
}

